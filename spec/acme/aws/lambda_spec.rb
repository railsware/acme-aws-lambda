# frozen_string_literal: true

RSpec.describe Acme::Aws::Lambda do
  it 'has a version number' do
    expect(Acme::Aws::Lambda::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
